# Task: longest_common_prefix

Implement a function with this exact signature in `solution.py`:

```python
def longest_common_prefix(strs):
    ...
```

**Spec:** Given a list of strings `strs`, return the longest string that is a prefix of every
string in the list. If there is no common prefix, or `strs` is empty, return `""`.

**Constraints:**
- Strings may be empty.
- Comparison is case-sensitive.
