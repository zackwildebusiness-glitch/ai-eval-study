# Task: climb_stairs

Implement a function with this exact signature in `solution.py`:

```python
def climb_stairs(n):
    ...
```

**Spec:** You are climbing a staircase with `n` steps. Each time you can climb either 1 or 2
steps. Return the number of distinct ways to reach the top.

**Constraints:**
- `n >= 1`.
- `climb_stairs(1) == 1`, `climb_stairs(2) == 2`.
