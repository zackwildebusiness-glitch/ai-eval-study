# Task: length_of_longest_substring

Implement a function with this exact signature in `solution.py`:

```python
def length_of_longest_substring(s):
    ...
```

**Spec:** Given a string `s`, return the length of the longest substring (contiguous) without
repeating characters. The empty string returns `0`.

**Constraints:**
- Substring means a contiguous run of characters (not a subsequence).
- Aim for O(n) time.
